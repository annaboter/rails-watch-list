<%= simple_form_for(@list) do |f|%>
  <%= f.input :name%>
  <%# should be an option for the user to upload list cover image %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, "Create list"%>
<% end %>
